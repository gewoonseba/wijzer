# Copy to apps/web/.env.local (or repo root .env.local and symlink)
AI_GATEWAY_API_KEY=

# Convex (after `npx convex dev` — copy CONVEX_URL to NEXT_PUBLIC_CONVEX_URL)
# CLIP_STORE=convex
# NEXT_PUBLIC_CONVEX_URL=

# Test / CI only: return a canned clip from POST /api/clips (no outbound fetch/AI)
# WIJZER_MOCK_CLIP=1
